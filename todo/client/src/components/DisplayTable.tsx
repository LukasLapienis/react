import { DataType } from '../interfaces/DataInterfaces';

export const DisplayTable = ({ data }: { data: DataType }) => {
  return (
    <table>
      <tr>
        <td>sveiki</td>
        <td>sveiki</td>
      </tr>
    </table>
  );
};
